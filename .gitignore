# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# training run-output dirs at the repo root (NOT the bioreef/training source
# package or scripts/training). Anchored to root so it can't swallow packages.

__pycache__/
*.pt
context/
annotations/
extract_frames.py
outputs/
models/
# graphify knowledge-graph outputs (local build artifacts)
graphify-out/
data/
models/